# API Backend (apps/api)
# Required for cron job authentication
CRON_SECRET=your-secure-random-string-here

# Vercel KV is automatically provisioned when deploying to Vercel
# For local development, you can use Vercel CLI to link your project:
# vercel link && vercel env pull .env.local

# Optional: For local development without Vercel KV
# KV_URL=redis://localhost:6379
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=
